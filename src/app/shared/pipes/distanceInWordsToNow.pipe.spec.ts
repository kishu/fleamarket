import { DistanceInWordsToNowPipe } from './distanceInWordsToNow.pipe';

describe('DistanceInWordsToNowPipe', () => {
  it('create an instance', () => {
    const pipe = new DistanceInWordsToNowPipe();
    expect(pipe).toBeTruthy();
  });
});
